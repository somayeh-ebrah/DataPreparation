# Python dependencies for the DataPreparation neuroimaging pipelines.
#
# NOTE: Several pipeline steps also require external (non-pip) neuroimaging
# tools that must be installed separately. See README.md / USER_GUIDE.md:
#   - FSL          (slicetimer, mcflirt, bet, fslmaths, flirt, fnirt, melodic, ...)
#   - ANTs         (antsBrainExtraction.sh and the antspyx Python bindings below)
#   - HD-BET       (deep-learning brain extraction; installed via pip below)
#   - dcm2niix     (optional, used by some DICOM->NIfTI paths)

# Core scientific stack
numpy>=1.21.0
pandas>=1.3.0

# Medical imaging I/O and processing
nibabel>=3.2.0
pydicom>=2.3.0
dicom2nifti>=2.4.0
SimpleITK>=2.2.0
antspyx>=0.3.0
nilearn>=0.9.0

# Brain extraction (deep learning)
HD-BET>=1.0.0

# Utilities
tqdm>=4.62.0
